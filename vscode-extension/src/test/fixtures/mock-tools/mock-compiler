#!/bin/bash
# Mock compiler for integration tests.
# Handles --version and common compiler subcommands.
case "$1" in
    --version) echo "0.0.1"; exit 0;;
    compile)   echo "Compiled successfully"; exit 0;;
    run)       echo "Running..."; exit 0;;
    test)      echo "All tests passed"; exit 0;;
    *)         exit 0;;
esac
